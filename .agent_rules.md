# Strict Zero Drift Policy
1. AG is forbidden from navigating outside the project root.
2. AG must not execute deployment commands without explicitly sourcing environment variables.
3. All structural changes must be approved by the Brain.

# CRITICAL: IAM & GCP AUTHENTICATION BOUNDARIES
- Under NO circumstances shall the agent utilize or fall back to the root identity (jgmander@gmail.com) for execution, deployment, or secret extraction.
- All GCP operations MUST use the dedicated service account: ag-agent@dbomar-post-mvp.iam.gserviceaccount.com.
- If the service account encounters a PERMISSION_DENIED error, DO NOT attempt to bypass it. You must Fail Loudly, output the exact IAM error, and halt execution so the Architect can adjust the GCP IAM bindings (e.g., granting roles/secretmanager.secretAccessor).
