# Agent Rules & Architecture Directives

## Zero Drift Policy
Under no circumstances are you permitted to navigate outside this workspace structure. Your scope of action is strictly contained within `/Users/jasonmander/dbomar-post/`.

## Pre-Flight Mandate
Before executing any `gcloud`, `firebase`, or `fastapi` deployment commands, you MUST run `./scripts/verify_env.sh` and ensure it exits with code `0`. If the script fails, you are strictly prohibited from proceeding with any deployment or execution steps.

## Zero-Trust Identity Mandate
All future GCP or Firebase interactions MUST be performed using the local vaulted service account (`ag-agent@dbomar-post-mvp.iam.gserviceaccount.com`). You are strictly forbidden from using any other global or fallback credentials in this workspace.
